services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: support-agent-backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Persist the FAISS index and knowledge base between restarts
      - ./backend/faiss_index:/app/backend/faiss_index
      - ./backend/knowledge_base:/app/backend/knowledge_base
      - ./backend/agent_config.json:/app/backend/agent_config.json
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    container_name: support-agent-frontend
    ports:
      - "8501:8501"
    env_file:
      - .env
    environment:
      - BACKEND_URL=http://backend:8000
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped
